/** Default Conveyor profile URL shown before a branded Trust Center domain is live. */
export const DEFAULT_TRUST_CENTER_PROFILE_DISPLAY = 'app.conveyor.com/profile/mediacore';

export const DEFAULT_TRUST_CENTER_PROFILE_URL = `https://${DEFAULT_TRUST_CENTER_PROFILE_DISPLAY}`;

/** Branded Trust Center domain once the customer is set up to publish. */
export const BRANDED_TRUST_CENTER_DISPLAY = 'trust.mediacore.com';

export const BRANDED_TRUST_CENTER_URL = `https://${BRANDED_TRUST_CENTER_DISPLAY}`;
